class Solution {
public:
    bool isPalindrome(int x) {
     long long b=0;
     long long k=x;
     while(x>0){
   
     long long a = x%10;
         b = b*10 + a;
         x=x/10;
     }

     if(k==b){
         return true;
     }
     else {
         return false;
     }


    }
};
